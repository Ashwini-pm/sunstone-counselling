// Types for lib/emailTemplates.mjs, which is plain JS so that a plain node
// script can import it without a TypeScript loader.
export function plainText(name: string): string
export function html(name: string): string
export function firstName(name: string): string
